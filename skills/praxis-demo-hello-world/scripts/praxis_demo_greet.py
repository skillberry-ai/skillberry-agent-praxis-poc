def praxis_demo_greet(name: str):
    """
    Return a personalised greeting for the given name.

    Args:
        name (str): The name of the person to greet, e.g. 'Alice'.

    Returns:
        str: A greeting message, e.g. 'Hello, Alice!'.
    """
    return f"Hello, {name}!"
